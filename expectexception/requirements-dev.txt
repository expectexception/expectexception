# Dev-only tooling: formatting, linting. Not installed on Render/production —
# see requirements.txt for that. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
black>=25.1.0
ruff>=0.16.0
